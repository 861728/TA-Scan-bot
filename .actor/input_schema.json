{
    "title": "M7 BottomFinder Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "mode": {
            "title": "Run Mode",
            "type": "string",
            "description": "Run mode: 'scan' for one-time scan, 'forever' for scheduled loop",
            "default": "scan",
            "enum": ["scan", "forever"]
        }
    }
}
